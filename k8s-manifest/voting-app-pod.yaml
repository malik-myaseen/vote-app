apiVersion: v1
kind: Pod
metadata:
  name: voting-app-pod
  labels:
    name: voting-app-pod
    app: demo-voting-app

spec:
  containers:
    - name: voting-app 
      image: malikyasin/vote-image:latest
      ports:
        - containerPort: 80